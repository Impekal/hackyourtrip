[pytest]
# Nur das Paket-Testverzeichnis einsammeln. bahn-local/ enthaelt ein
# eigenstaendiges Skript-Test (kein pytest-Stil, ruft sys.exit) - ohne diese
# Grenze wuerde `pytest` im Repo-Wurzelverzeichnis (so laeuft der Cron-Job)
# darueber stolpern.
testpaths = tests
